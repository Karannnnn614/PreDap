# Pinned to exact, known-good versions (verified installed in dev env).
# beautifulsoup4 and requests were removed: a grep over api_server/ confirmed
# neither is imported anywhere in server.py or logic.py.
# Versions bumped past pip-audit CVEs: Pillow (CVE-2026-25990 et al.),
# python-dotenv (CVE-2026-28684), and FastAPI pulls patched Starlette>=1.3.1.
fastapi==0.138.0
google-generativeai==0.8.5
pydantic==2.12.5
Pillow==12.2.0
uvicorn[standard]==0.34.0
python-dotenv==1.2.2
tenacity==9.1.4
# Pinned transitive dep: Starlette<1.3.1 has multiple CVEs (CVE-2026-54282/3 etc).
starlette==1.3.1
