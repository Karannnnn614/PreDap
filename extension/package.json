{
  "name": "predap-extension",
  "version": "1.0.0",
  "private": true,
  "description": "PreDAP Chrome (MV3) extension — query runner.",
  "type": "commonjs",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
